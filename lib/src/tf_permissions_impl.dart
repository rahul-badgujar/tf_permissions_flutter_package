import 'package:flutter/material.dart';

class TfPermissions extends StatelessWidget {
  const TfPermissions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
